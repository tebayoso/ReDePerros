<p>
  <span class='st_facebook_large' displayText='Facebook' st_url="<%= "http://redeperros.com/advertises/#{advertise.id}" %>" st_title="<%= "ReDePerros: #{advertise.status.capitalize}" %>" st_image=<%= URI.join(request.url, advertise.image.url) %> st_summary="<%= "#{advertise.comments}" %>"></span>
  <span class='st_twitter_large' displayText='Tweet' st_url="<%= "http://redeperros.com/advertises/#{advertise.id}" %>" st_title="<%= "ReDePerros: #{advertise.status.capitalize}" %>" st_image=<% URI.join(request.url, advertise.image.url) %> st_summary="<%= "#{advertise.comments}" %>"></span>
  <span class='st_googleplus_large' displayText='Google +' st_url="<%= "http://redeperros.com/advertises/#{advertise.id}" %>" st_title="<%= "ReDePerros: #{advertise.status.capitalize}" %>" st_image=<%= URI.join(request.url, advertise.image.url) %> st_summary="<%= "#{advertise.comments}" %>"></span>
  <span class='st_email_large' displayText='Email' st_url="<%= "http://redeperros.com/advertises/#{advertise.id}" %>" st_title="<%= "ReDePerros: #{advertise.status.capitalize}" %>" st_image=<%= URI.join(request.url, advertise.image.url) %> st_summary="<%= "#{advertise.comments}" %>"></span>
</p>